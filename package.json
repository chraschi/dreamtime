{
  "name": "chraschi-dreamtime",
  "version": "0.1.0",
  "engines": {
    "node": "^10.0.0"
  },
  "dependencies": {
    "yarn": "^1.22.0"
  },
  "scripts": {
    "postinstall": "cd src && yarn install"
  }
}
